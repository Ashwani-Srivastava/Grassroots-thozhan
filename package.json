{
  "name": "grassroots-first",
  "private": true,
  "version": "0.0.1",
  "description": "grassroots-first",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@ionic/core": "^5.3.2",
    "@stencil/core": "^2.0.1",
    "@types/jquery": "^3.5.5"
  },
  "dependencies": {
    "jquery": "^3.5.1",
    "owl.carousel": "^2.3.4"
  }
}
